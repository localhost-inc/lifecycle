# Archived Milestones

These milestone specs are retained for historical context after their active contracts were either shipped, superseded, or folded into current reference docs.

Archived here:

1. M0 monorepo bootstrap
2. M1 desktop shell
3. M2 local workspaces
4. M3 terminal runtime
5. M4 workspace environments

Do not treat these files as active implementation contracts. When a historical milestone contains durable rules that still matter, move those rules into `docs/reference/*` instead of reviving the milestone doc.
