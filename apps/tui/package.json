{
  "name": "@lifecycle/tui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "dev": "cargo watch -w src -c --no-process-group -s 'cargo run -p lifecycle-tui; stty sane'"
  }
}
