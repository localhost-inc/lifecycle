{
  "name": "@lifecycle/tui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "dev": "bun scripts/dev.ts"
  },
  "dependencies": {
    "@lifecycle/bridge": "workspace:*"
  }
}
