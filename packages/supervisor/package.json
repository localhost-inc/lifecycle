{
  "name": "@lifecycle/supervisor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "format": "oxfmt src",
    "lint": "oxlint src",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lifecycle/contracts": "workspace:*",
    "@lifecycle/stack": "workspace:*"
  }
}
